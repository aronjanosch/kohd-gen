/** Geometry constants at base scale (SVG user units). */

/** Node-center spacing on the 3x3 grid. */
export const PITCH = 110;
/** Canvas margin around the grid (room for perimeter routes + indicators). */
export const MARGIN = 70;
/** Node circle radius. */
export const R_NODE = 15;
/** Radial gap between a node circle and each revisit ring. */
export const RING_GAP = 7;
/** Clearance between the outermost ring and any passing trace. */
export const CLEAR = 7;
/** Spacing between parallel trace lanes. */
export const LANE = 9;
/** Stroke width for traces and circles. */
export const STROKE = 2.5;
/** Subnode dot radius. */
export const DOT_R = 3.75;
/** Arc-length spacing between dots within a group. */
export const DOT_SPACING = 10;
/** Arc-length gap between consecutive dot groups. */
export const GROUP_GAP = 14;
